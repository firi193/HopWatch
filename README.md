# HopWatch
HopWatch is a prompt injection detection and monitoring pipeline for AI agent chains. It watches what external content agents fetch and what tools they call, then decides whether a session looks like an attack.
